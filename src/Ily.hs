module Ily (
           ily,
           getIlyDirectory
           ) where

import System.Directory
import System.Environment (getArgs)

import Ily.Configuration


ily :: [String] -> IO ()
ily = undefined

